import { CodeToPositionPipe } from './code-to-position.pipe';

describe('CodeToPositionPipe', () => {
  it('create an instance', () => {
    const pipe = new CodeToPositionPipe();
    expect(pipe).toBeTruthy();
  });
});
